const initState = { display: "", result: "" };

const calculate = (state = initState, action) => {
  switch (action.type) {
    case "ADD":
    default:
      return state;
  }
};

export default calculate;
